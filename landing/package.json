{
  "name": "kindling-landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-assets": "node ../scripts/sync-assets.mjs",
    "dev": "npm run sync-assets && astro dev",
    "build": "npm run sync-assets && astro build",
    "check": "npm run sync-assets && astro check",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "7.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "typescript": "6.0.3"
  }
}
